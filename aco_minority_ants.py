import aco_routing

class Minority_Ants(aco_routing.Routing):
    def run(self):
        super().run()
    #    while not self.stop_event.is_set():
    #        # print(self.G)
    #        time.sleep(2)